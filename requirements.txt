# Core app
streamlit>=1.36.0
# starlette 1.4.0 added a required kwarg to GZipResponder.__init__ that
# streamlit 1.61.0 does not pass, crashing the server on startup.
# Remove this pin once streamlit ships a fix.
starlette<1.4
pandas>=2.2.0
numpy>=1.26.0

# Excel reading/writing
openpyxl>=3.1.2
XlsxWriter>=3.1.2

# Optional performance/caching (included with Streamlit)
blinker>=1.7.0

# For base64 encoding used in download links

# DOCX generation
python-docx>=1.1.0

pydeck

gspread
google-auth